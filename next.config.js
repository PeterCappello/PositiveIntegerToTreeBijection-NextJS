/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/PositiveIntegerToTreeBijection-NextJS',
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
